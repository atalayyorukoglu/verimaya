{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", "build/**", ".svelte-kit/**"]
		},
		"dev": {
			"cache": false,
			"persistent": true
		},
		"lint": {
			"dependsOn": ["^build"]
		},
		"check": {
			"dependsOn": ["^build"]
		},
		"test": {
			"dependsOn": ["^build"],
			"cache": false,
			"passThroughEnv": [
				"DATABASE_URL",
				"DATABASE_URL_APP",
				"CREDENTIALS_ENCRYPTION_KEY"
			]
		},
		"format": {
			"cache": false
		}
	}
}
