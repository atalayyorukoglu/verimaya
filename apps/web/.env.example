# Copy to apps/web/.env for local overrides.

# NestJS API origin (used when PUBLIC_USE_MSW=false)
PUBLIC_API_URL=http://localhost:3000

# Demo default: MSW mocks /v1/* in dev. Set to false to hit the real API.
PUBLIC_USE_MSW=true
